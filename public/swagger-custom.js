// Custom Swagger UI initialization
window.addEventListener('load', function() {
  console.log('Swagger UI loaded successfully');
  
  // Ensure Swagger UI is properly initialized
  if (window.ui) {
    console.log('Swagger UI instance found');
  }
}); 